# Runtime note:
# - `llama.cpp` uses Metal on Apple Silicon, or Vulkan/CPU on other platforms.
# - The ONNX backends default to DirectML and fall back to CPU when DML is unavailable.

click
librosa
mido
numpy<2.0.0
PyQt5
PyQt-Fluent-Widgets
PyYAML
scipy>=1.10.0
onnxruntime-directml; sys_platform == "win32"
onnxruntime; sys_platform != "win32"
soundfile
textgrid
tqdm
pyopenjtalk
